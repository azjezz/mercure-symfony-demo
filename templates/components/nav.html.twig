<nav class="bg-white border-b border-gray-100" x-data="{ open: false, mopen: false }">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
            <div class="flex">
                <div class="flex-shrink-0 flex items-center">
                    <a class="text-gray-900" href="{{ path('index') }}">ChitChat</a>
                </div>
            </div>
            <div class="hidden sm:flex sm:items-center sm:ml-6">
                {% if app.user %}
                    {% include 'components/navbar/items/username.html.twig' %}
                {% else %}
                    <a href="{{ path('user_login') }}" class="navbar-item">{{ 'account.login'|trans }}</a>
                    <span class="navbar-muted-item">/</span>
                    <a href="{{ path('user_register') }}" class="navbar-item">{{ 'account.register'|trans }}</a>
                {% endif %}
            </div>
            {% include 'components/navbar/responsive/menu.html.twig' %}
        </div>
    </div>
    {% include 'components/navbar/responsive/dropdown.html.twig' %}
</nav>